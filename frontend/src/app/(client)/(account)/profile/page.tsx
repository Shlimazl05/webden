import { ProfileForm } from "@/features/customer/components/client/ProfileForm";

/**
 * Trang thông tin cá nhân (Mặc định khi vào /profile)
 */
export default function ProfilePage() {
  return (
    <section>
      <ProfileForm />
    </section>
  );
}